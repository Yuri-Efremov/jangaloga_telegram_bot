BOT_TOKEN=put-your-telegram-bot-token-here

# Temporary directory for downloaded voice files (optional; defaults to ./tmp)
DATA_DIR=./tmp

# Dictionary path used by the bot (must exist; generated by build_dictionary.py)
DICT_PATH=./dictionary.json

# Single fixed reference voice (wav) used for all synthesis
SPEAKER_WAV=./speaker.wav

# Speech tempo for output audio (ffmpeg atempo): 1.0=normal, 0.67 ≈ 1.5x slower
SPEECH_TEMPO=0.67

# Telegram upload timeout (seconds). Increase if voice messages sometimes fail to send.
TELEGRAM_TIMEOUT=300

# Input limits
MAX_TEXT_CHARS=350
MAX_VOICE_SECONDS=30

# Healthcheck HTTP port for container platforms (readiness probe)
PORT=8080

# Whisper model size for ASR (faster-whisper): tiny|base|small|medium|large-v3
WHISPER_MODEL=small

# XTTS v2 model name for Coqui TTS
TTS_MODEL=tts_models/multilingual/multi-dataset/xtts_v2

# Language code for XTTS (ru recommended). Your Jangaloga uses Cyrillic, so ru phonetics works well.
TTS_LANGUAGE=ru


